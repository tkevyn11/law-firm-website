# Public site URL (no trailing slash).
# Leave unset in production — the code defaults to the canonical origin below.
# Only set this to point a preview/staging build at a different origin.
NEXT_PUBLIC_SITE_URL=https://www.tkalegal.my

# Resend email delivery (https://resend.com)
RESEND_API_KEY=
CONTACT_TO_EMAIL=general@tkalegal.com
CONTACT_FROM_EMAIL=Enquiry <onboarding@resend.dev>
